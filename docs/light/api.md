## API 同步
点亮支持将日志通过 API 同步到 flomo、inBox 等笔记平台，只需要你在 App 通用设置中设置 Api 即可，如下所示：

<p style="float: left;"><img src="../public/img/light_api.jpg" width="60%" alt=""></p>

在这里设置好 API 后，打卡时，如果记录了日志，日志会通过 API 同步到对应的笔记平台。

并且日志里会默认增加标签，标签格式为 #点亮 + 打卡名称，如 #点亮/俯卧撑。